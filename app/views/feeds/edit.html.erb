<%= form_with(model: @feed, local: true) do |form| %>
  <div class="wrapper">
    <h1>編集ページ</h1>
  </div>
  
    <%= render "form", feed: @feed %>

    <br>

  <p class="btn-post"><%= link_to "投稿一覧に戻る", feeds_path %></p>
